{
  "name": "desafio-final",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^4.0.0",
    "parcel": "^2.5.0"
  },
  "scripts": {
    "dev": "parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /piedra-papel-tijeras",
    "deploy": "gh-pages -d build"
  }
}
